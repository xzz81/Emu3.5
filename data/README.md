# data/

Project-facing data entry points live here.

- Put benchmark dataset entry points under `data/benchmarks/<name>`.
- Use symlinks when data already exists elsewhere on the server.
- Do not commit datasets, generated data, or server-specific symlinks.
- Record benchmark datasets in `benchmark_registry.yaml`.
