# external/benchmarks/

Cloned third-party benchmark or baseline repositories live here.

- Do not put external benchmark repositories under `data/`.
- Do not edit third-party repositories directly unless explicitly creating a fork or patch.
- Expose only dataset entry points to project code through `data/benchmarks/<name>`.
- Record each external benchmark repository in `benchmark_registry.yaml`.
